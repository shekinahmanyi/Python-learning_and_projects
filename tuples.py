# Tuples
dimensions = (200, 50) # Tuples are immutable lists and the difference is that they use parentheses instead of square brackets
print("Original dimensions:")
for dimension in dimensions:
    print(dimension)

dimensions = (400, 100) # You can assign a new value to a variable that holds a tuple
print("\nModified dimensions:")
for dimension in dimensions:
    print(dimension)
